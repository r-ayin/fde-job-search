# 岗位匹配度筛选流程（SOP）

> 建立时间：2026-09-10
> 适用：BOSS直聘 FDE / AI 解决方案类岗位批量筛选
> 硬性前提：**必须先获取 JD 正文，再进行评分**（列表页标题信息量不足，会误判）

---

## 流程总览

```
① 抓取岗位列表  →  ② 逐岗抓取 JD 正文  →  ③ 按 JD 评分  →  ④ 输出高分清单  →  ⑤ 生成文案（先确认再发）
```

---

## ① 抓取岗位列表

**入口**：`https://www.zhipin.com/web/geek/jobs?city=101210100&query=fde`（杭州 + 关键词）

**DOM 选择器**（Vue 2 页面，实测有效）：

| 字段 | 选择器 |
|---|---|
| 岗位卡片 | `li.job-card-box` |
| 岗位名 | `a.job-name` |
| 薪资 | `.job-salary` |
| 学历/经验标签 | `.tag-list li` |
| 公司名 | `.boss-name` |
| 区域 | `.company-location` |
| 岗位 ID | `a.job-name[href]` 提取 `/job_detail/{encryptJobId}.html` |

**注意事项**：
- 首屏只渲染部分卡片，需 `window.scrollTo(0, document.body.scrollHeight)` 滚到底再抓，重复 2-3 次
- 同一 `encryptJobId` 会因虚拟列表重复出现，**必须按 eid 去重**
- 脚本：`scrape4.js`（抓取）+ `scroll_more.js`（滚动）

---

## ② 逐岗抓取 JD 正文

**接口方式不可靠**：`/wapi/zpgeek/job/detail.json` 有频率风控，连续约 6 次后返回 `code:37 您的环境存在异常`。

**可靠方式**：用 CDP `Page.navigate` 真实导航到 `https://www.zhipin.com/job_detail/{eid}.html`，等待 6 秒后解析 DOM。

| 字段 | 选择器 |
|---|---|
| 是否有正文 | `.job-sec-text` 存在 |
| 岗位名 | `.name h1` 或 `h1` |
| 薪资 | `.salary` |
| **JD 正文** | `.job-sec-text` |
| 技能标签 | `.job-keyword-list li` |

**节奏**：每岗间隔 6 秒，实测 100% 成功。脚本：`fetch_all_jd.py`。

**排除**：`元/天` 的实习/日结岗位不纳入评分。

---

## ③ 按 JD 评分（总分 100）

> ⚠️ 评分输入必须是 **JD 正文**，不能只用列表页标题。

| 维度 | 权重 | 评分规则 |
|---|---|---|
| **岗位方向** | 30 | 标题含 FDE/前沿部署/前线部署/前向部署 → 30；含交付/实施/解决方案/部署 → 22；产品/项目/运营/顾问 → 12；销售/商务 → 4；其他 → 8 |
| **能力要求匹配** | 25 | JD 正文命中用户确认能力关键词，每项 +4，上限 6 项（24 分）。关键词见下 |
| **行业匹配** | 15 | 含电商/品牌/跨境 → 15；零售 → 8 |
| **薪资匹配** | 15 | 区间落在 15-30K → 15；上限 ≥15K → 10；否则 3 |
| **经验门槛** | 10 | 经验不限/1-3年/1年以内 → 10；3-5年 → 6；5-10年 → 1 |
| **排除项扣分** | 每项 -6 | 命中用户明确短板关键词（见下） |

### 用户确认的能力关键词（+4/项）
`mcp`、`docker`、`agent`、`智能体`、`rag`、`检索`、`向量`、`prompt`、`提示词`、`大模型`、`llm`、`微调`、`sft`、`部署`、`交付`、`解决方案`、`电商`、`tob`、`客户`、`方案`、`poc`、`工作流`、`记忆`、`知识库`、`评测`、`标注`

### 用户明确短板关键词（-6/项）
`spring boot`、`spring cloud`、`微服务架构`、`vue`、`react`、`typescript`、`kubernetes`、`k8s`、`java`、`golang`、`go语言`、`c#`、`全栈开发`

### 分档建议

| 分数 | 判定 | 动作 |
|---|---|---|
| ≥60 | 高度匹配 | 优先投递 |
| 45-59 | 较匹配 | 可以投递 |
| 30-44 | 一般 | 视情况，需人工判断 |
| <30 | 低匹配 | 跳过 |

**脚本**：`score.py`

---

## ④ 输出高分清单

排序输出，附评分理由（每个加减分项），存为 `search_scored.json` + Markdown 表格。

---

## ⑤ 生成文案（先确认再发）

- 必须读 `FDE岗位信息wiki.md` 第六节标准模板
- 4 点结构、约 460 字
- **逐条核对 JD 要求，只写有把握的**
- **写好先给用户确认，不得直接发**

---

## 已知坑

1. **F12 被禁用** → 走 OpenClaw 扩展 + 本地 relay 的 CDP 通道
2. **接口频率风控** → 改用真实导航
3. **虚拟列表重复渲染** → 按 eid 去重
4. **ZCode 抢焦点** → 键盘事件不可靠，用 CDP 或 UIA
5. **列表页信息不足** → 必须抓 JD 正文再评分
